import { Product } from "../types/product";

export const getProductColor = (A: Product) => A.color;
export const getProductPrice = (A: Product) => A.price;
export const getProductName = (A: Product) => A.name;
